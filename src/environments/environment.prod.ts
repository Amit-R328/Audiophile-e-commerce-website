export const environment = {
  production: true,
  // Full URL of your deployed Render backend, no trailing slash.
  // After the Render service is live, replace the host below with your real
  // service URL (e.g. https://<your-service-name>.onrender.com/api).
  apiUrl: 'https://audiophile-api.onrender.com/api'
};
